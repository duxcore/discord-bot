import { MiddlewareMethod } from "../structures/CommandExecutor";

export const confirmation = (conformationEmbed?: ) => {
  const mid: MiddlewareMethod = (client, interaction, next) => {
    
  }
  return mid;
}
